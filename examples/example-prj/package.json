{
  "private": true,
  "name": "example-prj",
  "version": "1.4.0",
  "scripts": {
    "bootstrap": "npm run generate",
    "migrate:test:ci": "DATABASE_URL=\"postgres://postgres:postgres@localhost:5432/postgres?schema=public\" prisma migrate dev",
    "generate": "prisma generate",
    "test": "jest",
    "test:ci": "DATABASE_URL=\"postgres://postgres:postgres@localhost:5432/postgres?schema=public\" jest"
  },
  "devDependencies": {
    "@quramy/jest-prisma": "*",
    "@prisma/client": "*",
    "@types/jest": "29.2.1",
    "prisma": "*",
    "jest": "29.2.2",
    "ts-jest": "29.0.3"
  },
  "dependencies": {
    "sqlite": "4.1.2"
  }
}
